{
  "name": "ts-command-queue",
  "version": "1.0.5",
  "main": "dist/index.js",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/jest": "^23.3.3",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.3",
    "tslint": "^5.11.0",
    "tslint-config-airbnb": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "typescript": "^3.1.1"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "checkall": "yarn tslint && yarn test",
    "clean": "rm -rf dist/*",
    "rebuild": "yarn clean; yarn build",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "https://rival.jfrog.io/rival/api/npm/npm/"
  },
  "publishConfig": {
    "registry": "https://rival.jfrog.io/rival/api/npm/npm-local/"
  },
  "author": "Zach Brown <zach@10etechnology.com>",
  "dependencies": {}
}
